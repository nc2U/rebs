apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "web.fullname" . }}-django-nfs-pvc
spec:
  storageClassName: app
  accessModes:
  {{- range .Values.volumes.djangoAccessModes }}
  - {{ . }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.volumes.djangoPvcStorage }}
