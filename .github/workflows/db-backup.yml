name: DB_BACKUP

on:
  push:
  schedule:
    - cron: "0 2 * * 0"

jobs:
  backup:

    name: DB_BACKUP
    runs-on: ubuntu-latest

    steps:
      - uses: actions-hub/kubectl@master
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        with:
          args: get pods -n prod-hrebs
